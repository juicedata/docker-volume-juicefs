version: '3'
services:
  busybox:
    image: busybox
    command: "ls /jfs"
    volumes:
      - jfsvolume:/jfs
volumes:
  jfsvolume:
    driver: juicedata/juicefs
    driver_opts:
      name: ${JFS_VOL}
      token: ${JFS_TOKEN}
      accesskey: ${ACCESS_KEY}
      secretkey: ${SECRET_KEY}
